#include <stdio.h> 

int main(){

// arithmetic operators
/*  + 
    -
    * (multiplication)
    / (division)
    % (modulus)
    ++ increment
    -- decrement

    */

int x = 5;
int y = 2;

int z = x / y; //returns int
float z = x / y; //returns float

float z = x / (float) y; 

int z = x % y; // remainder of division

x++; // returns 6
y--; // returns 1 

return 0;
}